using System;

namespace TurekSimulator
{
	/// <summary>
	/// Enum definiujący dostępne typy składników w grze.
	/// Wykorzystywany w logice zamówień, magazynie oraz systemie cen.
	/// </summary>
	public enum IngredientType
	{
		Bread,
		Meat,
		Sauce,
		Veggies
	}
}
